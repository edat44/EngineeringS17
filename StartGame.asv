function StartGame(handles)

%% Create both players with correct difficulty and strategy
rightPlayer = createPaddle(handles.easy, handles.balanced, handles.rightPlayerOffset, 0, 1, 2);

%% Create ball(s)

%% Start simulation
while true

end

%% Report game statistics
